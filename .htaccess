Options -Indexes

# Bloquear acceso directo a archivos sensibles (sintaxis Apache 2.4)
<FilesMatch "^\.env(\..*)?$">
    Require all denied
</FilesMatch>

<FilesMatch "\.(json|lock|md|gitignore)$">
    Require all denied
</FilesMatch>
